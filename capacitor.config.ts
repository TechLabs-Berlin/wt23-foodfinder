import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'wt23.foodfinder.ionic.react.app',
  appName: 'wt23-foodfinder-ionic-react-app',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
